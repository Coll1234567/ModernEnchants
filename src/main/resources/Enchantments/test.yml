name: 'test'
display-name: '&6Fancy&aWoo'

min-level: 1
max-level: 3
enchantment-target: BREAKABLE

actions:
  - BREAK_BLOCK

levels:
  1:
    effects:
      - lightning(user)
      - delay(100)
      - fake_lightning(user,0,10,0)