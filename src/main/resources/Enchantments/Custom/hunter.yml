name: 'hunter'
display-name: '<#576B4E>Hunter</#576B4E>'

description: '&7» Increases damage to animals.'
description-long: "&7Each level adds one heart of extra damage to animals."

weights:
  enchanting: 50
  trading: 100
  loot: 50

min-level: 1
max-level: 5

actions:
  - ATTACK_MOB
  
valid-items:
  - SWORDS
valid-items-anvil:
  - AXES

group: 'melee_damage_increase'

levels:
  1:
    min-enchanting-level: 0
    conditions:
      - is_animal(opponent,true)
    effects:
      - add_damage(2)
  2:
    min-enchanting-level: 6
    conditions:
      - is_animal(opponent,true)
    effects:
      - add_damage(4)
  3:
    min-enchanting-level: 12
    conditions:
      - is_animal(opponent,true)
    effects:
      - add_damage(6)
  4:
    min-enchanting-level: 18
    conditions:
      - is_animal(opponent,true)
    effects:
      - add_damage(8)
  5:
    min-enchanting-level: 24
    conditions:
      - is_animal(opponent,true)
    effects:
      - add_damage(10)